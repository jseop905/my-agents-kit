{{!--
  templates/CLAUDE.md.hbs — 패키지의 CLAUDE.md를 렌더하는 원본 템플릿.

  데이터 계약 (bin/build가 렌더 시 주입):
    kit_version : string — 생성 헤더에 박히는 kit 버전 (git describe)

  context 조각은 여기 주입하지 않는다 — 공통 지침은 AGENTS.md로 렌더되고,
  이 파일은 @AGENTS.md import로 그것을 상속한다. 여기엔 Claude Code 전용 지침만 둔다.

  생성물은 복사된 순간부터 프로젝트 소유다 — 보존 계약(kit:keep) 없음.
--}}
<!-- 생성: my-agents-kit {{kit_version}} — 공통 원본은 my-agents-kit repo의 templates/CLAUDE.md.hbs.
     복사 후 이 파일은 프로젝트 소유 — 자유롭게 수정해도 된다. 단 kit 업데이트는 새 패키지를
     통째로 덮어쓰는 방식이라, 프로젝트 수정분은 교체 후 git diff로 직접 복원한다. -->
# 프로젝트 지침 — Claude Code

@AGENTS.md

> 공통 작업 지침은 위 `AGENTS.md`에 있다(Codex 등 다른 도구와 공유). 공통 규칙은 여기 다시 쓰지 않는다 — 아래는 Claude Code 전용 지침만.

## Claude Code 지침

- **계획 승인은 플랜 모드로** — 공통 지침에서 계획·승인이 먼저인 작업("먼저 확인받는 작업", 설계 선택이 개입하는 큰 변경)은 플랜 모드로 계획을 제시하고 승인 후 구현한다.

## 프로젝트 노트 (Claude Code 전용)

(Claude Code에만 적용할 프로젝트 규칙·메모를 여기에 남긴다.)

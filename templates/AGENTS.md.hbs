{{!--
  templates/AGENTS.md.hbs — 패키지의 AGENTS.md를 렌더하는 원본 템플릿.

  데이터 계약 (bin/build가 렌더 시 주입):
    kit_version : string   — 생성 헤더에 박히는 kit 버전 (git describe)
    commands    : string   — 명령어 섹션 초기값 (profile의 commands 시드 또는 기본 placeholder)
    context     : string[] — agent-kit.yml의 context.include 파일들의 본문. 선언 순서 그대로 이어붙는다.

  생성물은 복사된 순간부터 프로젝트 소유다 — 보존 계약(kit:keep) 없음.
  kit 업데이트는 새 패키지로 통째 교체하고, 프로젝트 수정분은 git으로 복원한다.
--}}
<!-- 생성: my-agents-kit {{kit_version}} — 공통 원본은 my-agents-kit repo의 context/·templates/.
     복사 후 이 파일은 프로젝트 소유 — 자유롭게 수정해도 된다. 단 kit 업데이트는 새 패키지를
     통째로 덮어쓰는 방식이라, 프로젝트 수정분은 교체 후 git diff로 직접 복원한다. -->
# 프로젝트 지침

> AI 코딩 에이전트(Codex, Claude Code 등)가 이 프로젝트에서 따르는 공통 작업 지침.

## 명령어

```bash
{{commands}}
```

{{#each context}}
{{{this}}}
{{/each}}
## 프로젝트 노트

(프로젝트 고유 규칙·메모를 여기에 남긴다.)

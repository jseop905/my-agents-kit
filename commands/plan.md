---
name: plan
description: Plan a feature on an existing codebase — break the goal into small verifiable tasks with dependency ordering
---

Follow the workflow in `.claude/skills/planning-and-task-breakdown/SKILL.md`.

Enter plan mode first (read-only, no code changes), then execute the skill's process.

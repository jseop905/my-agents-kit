# my-agents-kit — Claude Code

@AGENTS.md

> 공통 개발 지침은 위 `AGENTS.md`(Codex와 공유). 아래는 Claude Code 전용 메모.

- 이 repo에서 `context`·`skills`·`templates`·`profiles`·`bin`·`agent-kit.yml`은 곧 **제품(배포물)**이다 — 편집이 곧 산출물 변경임에 유의.

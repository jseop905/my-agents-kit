## 테스트

- 리스크 기반으로 쓴다: 버그 수정(재현)·핵심/복잡 로직·공개 인터페이스가 우선. 단순 글루·일회성·자명한 코드는 생략할 수 있다.
- 버그 수정은 재현 테스트가 먼저다: 실패를 확인하고 → 근본 원인을 고치고 → 통과와 전체 회귀를 확인한다.
- 변경 후에는 영향 범위의 테스트를 실제로 돌린다. 돌리지 않은 테스트를 통과했다고 말하지 않는다.
- 테스트는 그것이 커버하는 코드와 같은 커밋에서 수정한다.
- 통과시키기 위해 테스트를 약화·삭제·스킵하지 않는다. 기대 동작이 정말 바뀌었을 때만 기대값을 바꾼다.

## 보안

- 시크릿(키·토큰·자격증명)을 코드·로그·커밋에 넣지 않는다. 이미 노출된 것을 발견하면 즉시 보고한다.
- 외부 입력(사용자 입력·API 응답·파일 내용)은 신뢰 경계에서 검증한다.
- 민감 데이터는 필요한 최소 범위로만 다루고, 출력·로그·에러 메시지에 노출하지 않는다.
- 의존성을 추가할 때는 출처와 유지 상태를 검증한다. 이름이 비슷한 위장 패키지에 주의한다.
- 인증·인가·암호화 로직은 직접 구현하지 않는다 — 검증된 표준 구현을 쓴다.
